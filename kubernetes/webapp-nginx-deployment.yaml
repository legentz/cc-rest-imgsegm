apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-nginx-deployment
  labels:
    app: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp-nginx-alpine
        image: 172.31.24.144:5001/webapp/nginx-alpine:v1.0
        ports:
        - containerPort: 80